# `class ndarray which is used to make tab (nd array for n dimension array)
# to be able to use the diff methods we first have to use the constructor named array`


import numpy as np

tableau = np.array([1, 2, 3])

print(tableau.size)
